# Copy this file to .streamlit/secrets.toml (same folder name, remove .example)
# and fill in your real connection string. NEVER commit the real secrets.toml
# to git - add ".streamlit/secrets.toml" to .gitignore.

DATABASE_URL = "postgresql://postgres:YOUR_PASSWORD@localhost:5432/expensemind"
